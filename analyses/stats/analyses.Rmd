---
output:
    html_document:
        theme: journal
        highlight: zenburn
---

```{r setup}
renv::activate()

library(here)
library(readr)
library(dplyr)

d_analysis <- readr::read_delim(
  here::here(
    "data",
    "analysis",
    "analysis-data.delim"
  ),
  delim = "|"
)

l_model_params <- readRDS(
  file = here::here(
    "data",
    "helpers",
    "l_model_params.RData"
  )
)
```
